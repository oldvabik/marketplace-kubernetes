apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: marketplace
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
