apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: marketplace
spec:
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
  type: ClusterIP
