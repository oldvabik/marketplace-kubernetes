apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: marketplace
spec:
  selector:
    app: user-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081
  type: ClusterIP
