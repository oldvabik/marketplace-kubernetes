apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: marketplace
spec:
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 8079
      targetPort: 8079
  type: LoadBalancer
